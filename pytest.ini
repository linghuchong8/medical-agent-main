[pytest]
# 让 pytest 从项目根目录启动，src.xxx 的 import 才能解析
pythonpath = .

# pytest-asyncio 模式：auto 表示所有 async 测试自动识别，无需手动加 @pytest.mark.asyncio
asyncio_mode = auto

# 所有 async 测试共享同一个事件循环（模块级 Redis/连接池等单例才能跨测试复用）
asyncio_default_test_loop_scope = session

# 测试文件发现目录
testpaths = test