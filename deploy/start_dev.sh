#!/usr/bin/env bash
# ============================================
# 天宫医疗智能体 - 一键部署+启动
# (全新机器自动完成部署, 已部署过则直接启动)
# 使用前: chmod +x start_dev.sh
# ============================================
set -e
DIR="$(cd "$(dirname "$0")" && pwd)"

echo "============================================"
echo " 天宫医疗智能体 - 一键部署+启动"
echo "============================================"

bash "$DIR/install.sh"
bash "$DIR/start.sh"
